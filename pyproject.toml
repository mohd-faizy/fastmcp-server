[project]
name = "fastmcp-server"
version = "0.1.0"
description = "A simple learning MCP server and agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.4.2",
    "groq>=1.5.0",
    "huggingface-hub>=1.20.1",
    "mcp>=1.28.0",
    "python-dotenv>=1.2.2",
]
